# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Install dependencies
      run: npm ci
    - name: Run ESLint
      run: npm run lint
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Install dependencies
      run: npm ci
    - name: Run tests
      run: npm test
  provision:
    runs-on: ubuntu-latest
    needs:
    - test
    env:
      DIGITALOCEAN_TOKEN: ${{ secrets.DO_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.SPACES_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SPACES_SECRET_KEY }}
      AWS_ENDPOINT_URL_S3: ${{ secrets.SPACES_ENDPOINT }}
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_do_token: ${{ secrets.DO_TOKEN }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - name: Terraform Init
      run: "host=\"${AWS_ENDPOINT_URL_S3#https://}\"\nterraform init -input=false\
        \ -reconfigure \\\n  -backend-config=\"bucket=${{ secrets.TF_STATE_SPACE }}\"\
        \ \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME }}/terraform.tfstate\"\
        \ \\\n  -backend-config=\"region=${host%%.*}\" \\\n  -backend-config=\"skip_credentials_validation=true\"\
        \ \\\n  -backend-config=\"skip_requesting_account_id=true\" \\\n  -backend-config=\"\
        skip_metadata_api_check=true\" \\\n  -backend-config=\"skip_region_validation=true\"\
        \ \\\n  -backend-config=\"skip_s3_checksum=true\" \\\n  -backend-config=\"\
        use_path_style=true\"\n"
      working-directory: infra
    - name: Terraform Apply
      run: terraform apply -input=false -auto-approve
      working-directory: infra
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    env:
      DIGITALOCEAN_TOKEN: ${{ secrets.DO_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.SPACES_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SPACES_SECRET_KEY }}
      AWS_ENDPOINT_URL_S3: ${{ secrets.SPACES_ENDPOINT }}
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_do_token: ${{ secrets.DO_TOKEN }}
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Install Ansible
      run: 'pip install ansible-core

        ansible-galaxy collection install community.general

        '
    - name: Write SSH key
      run: 'mkdir -p ~/.ssh

        printf ''%s\n'' "$SSH_KEY" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        '
      env:
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - name: Terraform Init
      run: "host=\"${AWS_ENDPOINT_URL_S3#https://}\"\nterraform init -input=false\
        \ -reconfigure \\\n  -backend-config=\"bucket=${{ secrets.TF_STATE_SPACE }}\"\
        \ \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME }}/terraform.tfstate\"\
        \ \\\n  -backend-config=\"region=${host%%.*}\" \\\n  -backend-config=\"skip_credentials_validation=true\"\
        \ \\\n  -backend-config=\"skip_requesting_account_id=true\" \\\n  -backend-config=\"\
        skip_metadata_api_check=true\" \\\n  -backend-config=\"skip_region_validation=true\"\
        \ \\\n  -backend-config=\"skip_s3_checksum=true\" \\\n  -backend-config=\"\
        use_path_style=true\"\n"
      working-directory: infra
    - id: get_ip
      name: Get Droplet IP
      run: echo "droplet_ip=$(terraform output -raw droplet_ip)" >> "$GITHUB_OUTPUT"
      working-directory: infra
    - name: Wait for SSH
      run: "for i in $(seq 1 30); do\n  if nc -z -w 5 \"${{ steps.get_ip.outputs.droplet_ip\
        \ }}\" 22; then\n    echo \"SSH is up\"; exit 0\n  fi\n  echo \"waiting for\
        \ sshd ($i/30)\"; sleep 10\ndone\necho \"sshd never came up\"; exit 1\n"
    - name: Scan host key
      run: ssh-keyscan -H "${{ steps.get_ip.outputs.droplet_ip }}" >> ~/.ssh/known_hosts
    - name: Generate inventory
      run: 'echo "[droplet]" > inventory.ini

        echo "${{ steps.get_ip.outputs.droplet_ip }} ansible_user=${{ secrets.SSH_USER
        }} ansible_ssh_private_key_file=~/.ssh/deploy_key" >> inventory.ini

        '
    - name: Run Ansible playbook
      run: ansible-playbook -i inventory.ini ansible/site.yml
  verify:
    runs-on: ubuntu-latest
    needs:
    - provision
    - configure
    env:
      DIGITALOCEAN_TOKEN: ${{ secrets.DO_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.SPACES_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SPACES_SECRET_KEY }}
      AWS_ENDPOINT_URL_S3: ${{ secrets.SPACES_ENDPOINT }}
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_do_token: ${{ secrets.DO_TOKEN }}
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - name: Terraform Init
      run: "host=\"${AWS_ENDPOINT_URL_S3#https://}\"\nterraform init -input=false\
        \ -reconfigure \\\n  -backend-config=\"bucket=${{ secrets.TF_STATE_SPACE }}\"\
        \ \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME }}/terraform.tfstate\"\
        \ \\\n  -backend-config=\"region=${host%%.*}\" \\\n  -backend-config=\"skip_credentials_validation=true\"\
        \ \\\n  -backend-config=\"skip_requesting_account_id=true\" \\\n  -backend-config=\"\
        skip_metadata_api_check=true\" \\\n  -backend-config=\"skip_region_validation=true\"\
        \ \\\n  -backend-config=\"skip_s3_checksum=true\" \\\n  -backend-config=\"\
        use_path_style=true\"\n"
      working-directory: infra
    - id: get_ip
      name: Get Droplet IP
      run: echo "droplet_ip=$(terraform output -raw droplet_ip)" >> "$GITHUB_OUTPUT"
      working-directory: infra
    - name: Health check
      run: "curl --fail --retry 10 --retry-delay 15 --retry-all-errors \\\n  http://${{\
        \ steps.get_ip.outputs.droplet_ip }}\n"
